apiVersion: v1
kind: Service
￼metadata:
  name: hello-world
￼spec:
  type: NodePort
  ￼ports:
    ￼- port: 80
      targetPort: http
      nodePort: 30080
      protocol: TCP
      name: http
  ￼selector:
    app: hello-world
